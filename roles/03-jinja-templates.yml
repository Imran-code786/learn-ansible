- name: Variable from Inventory
  hosts: TEST
  tasks:
    - name: Copy file
      ansible.builtin.file:
        src: sample.txt
        dest: /tmp/h1.txt

    - name: copy template
      ansible.builtin.template:
        src: sample.txt.j2
        dest: /tmp/h2.txt